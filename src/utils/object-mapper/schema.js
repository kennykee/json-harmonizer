const hotelMapping = {
  firstName: ["firstName", "first_name", "name.first", "fname"],
  lastName: ["lastName", "last_name", "name.last", "lname"],
  email: ["email", "emailAddress", "contact.email", "contact_email"],
};

export default hotelMapping;
